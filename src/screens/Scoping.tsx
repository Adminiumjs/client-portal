/**
 * Scoping: a worksheet that prices a stage before it becomes a proposal — to be drawn.
 *
 * A placeholder: it names the screen and nothing more. The screen's lane
 * replaces this file whole (same name, a default export, no props).
 */
import { Placeholder } from "../components/Placeholder.tsx";

export default function Scoping() {
  return <Placeholder titleKey="screen.scoping" view="scoping" />;
}
