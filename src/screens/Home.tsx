/**
 * The studio's Home: the day's lead, the three figures, aging, recent activity, waiting on a client, this week — to be drawn.
 *
 * A placeholder: it names the screen and nothing more. The screen's lane
 * replaces this file whole (same name, a default export, no props).
 */
import { Placeholder } from "../components/Placeholder.tsx";

export default function Home() {
  return <Placeholder titleKey="nav.home" view="home" />;
}
