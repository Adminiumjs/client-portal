/**
 * Enquire: the public form someone fills in to ask about new work — to be drawn.
 *
 * A placeholder: it names the page and nothing more. The page's lane
 * replaces this file whole (same name, a default export, no props).
 */
import { Placeholder } from "../../components/Placeholder.tsx";

export default function ClientEnquire() {
  return <Placeholder titleKey="screen.enquire" view="enquire" />;
}
